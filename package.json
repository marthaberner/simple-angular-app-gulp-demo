{
  "name": "reddit-clone-simple",
  "version": "1.0.0",
  "description": "- npm init to generate a `package.json` file",
  "main": "index.js",
  "dependencies": {
    "gulp": "^3.9.0"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-concat": "^2.6.0"
  },
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "gulp html && gulp scripts && gulp styles"
  },
  "author": "",
  "license": "ISC"
}
